from django.apps import AppConfig


class FitterConfig(AppConfig):
    """Configuration for the fitter app."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'fitter'
    verbose_name = 'AIFitter'
